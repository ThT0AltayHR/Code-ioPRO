# main.yml — Eski workflow (build_apk.yml tarafından kapsanmaktadır)
# Bu dosya build_apk.yml ile birleştirilmiştir.
# Ayrı tutulması gerekmez; yine de uyumluluk için boş bırakılmıştır.
name: "[Eski] CI — build_apk.yml kullanın"

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Yönlendirme
        run: echo "Lütfen build_apk.yml workflow'unu kullanın."
